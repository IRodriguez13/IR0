#pragma once 


/*

    El mismo main() que ves en tus To Do list, pero creado específicamente para mi caso de uso, que es centralizar el arranque del kernel.
    La idea es que main sea la funcion que inicia el kernel independientemente de la arquitectura  desde la que se disparó el boot.

*/
void main();