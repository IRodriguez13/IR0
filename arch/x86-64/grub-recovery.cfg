# Autoboot recovery for smoke-recovery / local recovery ISO.
set timeout=0
set default=0

menuentry "IR0 Kernel - Recovery (local console)" {
    multiboot /boot/kernel-x64.bin ir0.recovery=1 ir0.loglevel=normal
    set gfxpayload=text
    boot
}
