# IR0 0.0.1 — ABI contract board (userspace moderno)

> **Last verified:** 2026-07-09  
> **Source of truth:** `scripts/linux_abi/contracts.json`, `make linux-abi-audit`,  
> `Documentation/ai_driven_dev/linux_ground_truth.md`, `make smoke-release-0.0.1`

Estados por iteración (regla local `~/.cursor/rules/kernel-linux/ir0-contract-iteration-states.mdc`):

| Estado | Significado |
|--------|-------------|
| **VERIFIED** | Audit Linux automatizado PASS + contrato enabled |
| **LINUX-LIKE** | ktest/smoke OK; audit pendiente o parcial |
| **BLOCKED** | Primera divergencia Linux↔IR0 documentada |
| **EXPERIMENTAL** | Fuera del certificado 0.0.1 (TCC, Doom, etc.) |

## Resumen

| Estado | Contratos | % cola 0.0.1 |
|--------|-----------|--------------|
| **VERIFIED** | 15 | 14/15 ≈ **93%** (cola + wait4_wnohang + vfs_write) |
| **LINUX-LIKE** | 1 | dup/dup2 sin audit dedicado |
| **BLOCKED** | 0 | — |
| **EXPERIMENTAL** | 2 | no cuenta para rc/final |

**Cola prioritaria 0.0.1 (15):** brk, wait4, read, mmap, mount, execve, open/close, stat/fstat, dup/dup2, pipe, poll, nanosleep, getcwd, unlink/rename, chdir.

**Bundle R3 `vfs_write`** cubre en un solo audit: open O_CREAT/TRUNC/APPEND/EXCL, write/read roundtrip, lseek, unlink, rename, mkdir/rmdir, truncate/ftruncate — puente hacia MINIX rw (FAT16 rw solo tras re-ejecutar el mismo bundle).

**Faltan para certificación audit completa de la cola:** 1 (`dup`/`dup2`).

---

## VERIFIED (audit PASS)

| Contrato | Gate | Notas |
|----------|------|-------|
| brk | `linux-abi-audit` / `diag-contract CONTRACT=brk` | delta 4 KiB; VA absoluta puede diferir |
| wait4 | `linux-abi-audit-wait4` | fork + `_exit(42)` + status word |
| **wait4_wnohang** | `linux-abi-audit-wait4-wnohang` | WNOHANG alive → exitack handshake → block reap → ECHILD |
| read | `linux-abi-audit-read` | pipe/EOF/EBADF |
| **pipe** | `linux-abi-audit-pipe` / `diag-contract CONTRACT=pipe` | reusa `read_probe` + ktest `syscall_pipe` |
| **poll** | `linux-abi-audit-poll` | POLLIN on pipe; ktest `poll_resume_invariant` |
| **nanosleep** | `linux-abi-audit-nanosleep` | 2 ms sleep → ret 0 |
| **getcwd** | `linux-abi-audit-getcwd` | cwd `/` tras `chdir("/")` en probe |
| **chdir** | `linux-abi-audit-chdir` | `chdir("/")` + `getcwd` verify |
| mmap | `linux-abi-audit-mmap` | anon RW, PROT_NONE, MAP_FIXED, munmap, bad fd |
| mount | `linux-abi-audit-mount` | tmpfs `/tmp/ir0mnt`; FAT16 smoke separado |
| openat / close | `linux-abi-audit-openat` | `/proc/uptime`, `/sbin/ir0open_noent`, EBADF |
| stat / fstat | `linux-abi-audit-stat` | `/proc/uptime`, `/sbin/init`, ENOENT, fstat EBADF |
| execve | `linux-abi-audit-execve` | helper + ENOENT path |
| **vfs_write (R3 bundle)** | `linux-abi-audit-vfs-write` | 29/29 PASS; tmpfs `/tmp/ir0wtest`; runner MINIX format-large |

---

## LINUX-LIKE (ktest/smoke; audit pendiente)

| Contrato | Evidencia | Próximo paso |
|----------|-----------|--------------|
| dup / dup2 | ktests parciales, fd table | audit dedicado (probe + compare) |

*(unlink/rename/lseek/truncate individuales absorbidos por bundle `vfs_write`.)*

---

## BLOCKED

| Contrato | Divergencia | Fix mínimo |
|----------|-------------|------------|
| — | — | — |

---

## EXPERIMENTAL (no gate 0.0.1)

| Área | Motivo |
|------|--------|
| TCC in-guest | `smoke-fase52-tcc` cuelgue en link estático |
| Doom / tier-2 gfx | fuera del release gate D1.20 |

---

## Release gate vs audit

| Gate | Incluye audit |
|------|----------------|
| `make smoke-release-0.0.1` | `linux-abi-audit` (todos los enabled) |
| `make linux-abi-audit` | **15** contratos enabled (brk … chdir, wait4_wnohang, vfs_write, execve) |
| `make agent-fast` | host + ktest (~20s) |
| `make diag-contract CONTRACT=…` | ISO fresco + un contrato |

**FAT16 rw:** no habilitar hasta `linux-abi-audit-vfs-write` verde **sobre backend FAT16** (mismo ELF).

Ver plan: `Documentation/releases/IR0_0.0.1_VFS_WRITE_PLAN.md`.
